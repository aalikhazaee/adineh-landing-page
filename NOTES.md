# مستندات و ایده‌های شخصی در توسعه لندینگ پیج

در پیاده‌سازی این لندینگ پیج سعی کردم فراتر از یک قالب ساده عمل کنم و مواردی را در زمینه بهبود تجربه کاربری (UX)، گرافیک و مدیریت داده‌های ناقص API در نظر بگیرم.

## ۱. چالش‌های API و مدیریت داده‌ها
در حین اتصال به اندپوینت `https://adineh.market/wp-json/adineh/v1/adineh-stock/` با دو محدودیت در دیتای برگشتی مواجه شدم که به شرح زیر مدیریت شدند:

* **عدم وجود فیلد تصویر محصولات:** در ریسپانس دریافتی، هیچ فیلدی برای تصویر کالا (مثل `image` یا `thumbnail`) وجود نداشت. برای حفظ ساختار UI، تصاویر محصولات را به صورت موقت با یک تصویر Placeholder هندل کردم تا در صورت اضافه شدن این فیلد در آینده، قالب به هم نریزد.
    
* **مقدار Null برای قیمت فروش (`sell_price`):**
    فیلد `sell_price` که منطقاً باید قیمت کتاب دست‌دوم باشد، در تمام آبجکت‌ها مقدار `null` برمی‌گرداند (تنها قیمت روی جلد یا همان `cover_price` دارای مقدار بود). برای نمایش بهتر و نشان دادن منطق کاری‌ام، یک **Fallback Logic** در `main.js` پیاده‌سازی کردم:
    اگر قیمت فروش اصلی موجود نباشد، کد به صورت خودکار یک تخفیف فرضی (۳۰ درصد) روی قیمت روی جلد (`cover_price`) اعمال می‌کند تا تفاوت قیمت نو و دست‌دوم در کارت محصول به خوبی به کاربر نمایش داده شود.

## ۲. گرافیک و محتوای بصری
* **طراحی اختصاصی بنرها:** برای بنرهای اسلایدر صفحه اصلی (Hero Section) به جای استفاده از تصاویر آماده و کپی، بنرها را به‌صورت اختصاصی با ترکیب ابزار **نانو بنانا (Nano Banana)** و **ادوبی ایلوستریتور (Adobe Illustrator)** طراحی کردم تا کاملاً با هویت بصری، نیاز بیزینس و تم رنگی پروژه همخوانی داشته باشند.

## ۳. ایده‌های بصری و تجربه کاربری (UX/UI)
برای اینکه لندینگ پیج حس مدرن‌تر و کاربرپسندتری داشته باشد، موارد زیر را به پروژه اضافه کردم:

1.  **معماری Bento UI برای دسته‌بندی‌ها:** به جای نمایش لیستی خسته‌کننده، بخش "انتخاب پایه" را به سبک Bento Box طراحی کردم که با کلیک روی هر پایه، انیمیشن نرمی اجرا شده و زیرشاخه‌ها باز می‌شوند. این کار باعث صرفه‌جویی در فضای صفحه و جذابیت بصری می‌شود.
2.  **استایل Glassmorphism:**
    در هدر سایت و بخش‌های مختلف، از افکت شیشه‌ای (Backdrop blur + opacity) استفاده کردم که ترند روز طراحی است.
3.  **Navigation اختصاصی موبایل:**
    از آنجایی که هدر در موبایل فضای زیادی می‌گیرد، یک Bottom Navigation مشابه اپلیکیشن‌های نیتیو (Native Apps) برای کاربران موبایلی طراحی کردم که دسترسی به سبد خرید و دسته‌بندی‌ها را آسان‌تر می‌کند.
4.  **Drag-to-Scroll برای محصولات:**
    اسکرول افقی در دسکتاپ با ماوس همیشه سخت است، بنابراین منطق کلیک و کشیدن (Drag to scroll) را با جاوااسکریپت خالص برای بخش اسلایدر محصولات پیاده‌سازی کردم.